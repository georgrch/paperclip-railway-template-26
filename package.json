{
  "name": "paperclip-railway",
  "version": "1.0.0",
  "description": "Railway wrapper for paperclipai/paperclip with a web-based setup page",
  "private": true,
  "scripts": {
    "start": "node scripts/start.mjs",
    "build": "node scripts/patch-codex-costs.mjs",
    "test": "node --test tests/codex-costs.test.mjs",
    "test:integration": "node --test tests/adapter-integration.test.mjs"
  },
  "dependencies": {
    "paperclipai": "latest",
    "@anthropic-ai/claude-code": "latest",
    "@openai/codex": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
